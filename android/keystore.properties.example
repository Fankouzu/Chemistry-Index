# Copy to keystore.properties (gitignored) in this android/ directory.
# Release signing for Play Store — never commit keystore.properties or .jks files.

storeFile=../path/to/your-release-key.jks
storePassword=your-store-password
keyAlias=your-key-alias
keyPassword=your-key-password
