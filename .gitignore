node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
.sisyphus

# Gradle: IDE-generated daemon JVM pin (JetBrains JBR) breaks Linux CI toolchain download
android/gradle/gradle-daemon-jvm.properties

# Android release signing (never commit real secrets)
android/keystore.properties
*.jks
*.keystore

# Huawei AppGallery PEPK output (encrypted signing key — never commit)
sign.zip

# PEPK tool — download when needed; do not vendor in repo
pepk.jar

# Xcode / iOS local artifacts
*.xcuserstate
xcuserdata/